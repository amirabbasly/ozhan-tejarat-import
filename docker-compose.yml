version: "3.9"

services:
  postgres:
    image: postgres:16
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
      - static_volume:/backend-static:ro   # change mount point

    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: Dockerfile      # backend Dockerfile in project root
    env_file: .env
    volumes:
      - static_volume:/backend-static     # where collectstatic puts files
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build: ./frontend
    restart: unless-stopped

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx:/etc/nginx/conf.d:ro  
      - static_volume:/backend-static:ro
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    restart: unless-stopped

volumes:
  pgdata:
  static_volume:
